spring:
  application:
    name: eureka-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://configserver:8080

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
      maxThreadsForPeerReplication: 0