spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/users
    driverClassName: com.mysql.cj.jdbc.Driver
    password: 28012003Nastia
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    show-sql: 'true'
    hibernate:
      ddl-auto: create
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_HOST:http://localhost:8085/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: user-instance
server:
  port: 8070